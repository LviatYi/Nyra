# Nyra Roadmap

## 待开发需求

按当前优先顺序排列，仅保留准备近期处理的小目标。

- [ ] Reaction：接入 TS 运行器，支持直接加载、执行和回收脚本进程。
- [ ] Reaction：提供鼠标动作、等待和日志 SDK，打通 TS 与 Rust 的标准流协议。
- [ ] Reaction：实现指定屏幕位置的点击、按下、释放及等待，处理异常后的按钮释放。
- [ ] Sensory：按当前受关注 tip 注册全局快捷键，触发其脚本并呈现运行状态。
- [ ] Reaction：用配置和真实 TS 脚本验收轮转提示到鼠标宏与日志的完整循环。
- [ ] 配置：为轮转 tip 配置全局快捷键和 TS 脚本入口，保留原有纯提示配置。
- [x] 调整动画，使连续两次实际播放的涟漪起点至少相隔一定距离。
- [x] 替代文本截断方案，使长提示能够完整阅读。

## 当期 MVP

玩家通过 `config.json` 配置一个带快捷键和 TS 脚本的轮转 tip；该 tip
受关注时监听全局快捷键，按键后执行脚本。脚本支持指定屏幕位置的点击（按下后释放）、独立按下、独立释放、等待，以及向 Rust 宿主发送日志。

本期以这里的范围为准。[闭环分析文档](docs/detection-reaction-minimal-loop.md) 中的 Detection
条件等待、完整调试环境与分发方案保留为后续参考，不作为本期前置。

实施约定：

- “受关注”暂按当前 `ActiveJobState` 选中并展示的 tip 定义，与 Nyra 窗口是否获得系统焦点无关；不新增独立的关注操作。
- 虽称为键盘宏，本期按上述操作列表实现鼠标宏；首版按钮按左键定义，不扩展键盘组合键、滚轮或其他按钮。
- 坐标定义为 Windows 虚拟桌面的物理像素，原点为主屏左上角，其他屏幕可能使用负坐标；不是窗口客户区坐标或 Bevy 逻辑坐标。
- 点击、按下、释放都先将指针定位到指定坐标；在不同位置按下和释放会产生拖动语义，不默认恢复指针原位置。
- 仅当前受关注 tip 的触发键有效。一次触发固定本次脚本入口，后续 tip 轮转不会替换或重启正在执行的脚本；全局只运行一个脚本，忙时拒绝新触发、不排队。
- 配置启动时加载；每次启动脚本使用新进程读取已保存的脚本文件，无需重新编译 Nyra。脚本运行期间不热替换文件或自动重跑。

依赖顺序：配置确定后接入运行器；SDK/协议定义动作请求，再接入鼠标执行；当前 tip 的快捷键与展示接入配置和运行状态，最后完成真实场景验收。本期不依赖屏幕采集或模式识别。

## Configuration

### 当期 MVP：tip 与脚本关联

阶段目标：用户只需编辑 `config.json` 并提供 TS 文件，即可定义可触发的轮转提示。

- 保留现有 `tip/interval/showTime/color` 及轮转语义；为 tip 增加可选的应对配置，至少包含 `hotkey` 与 `script`，两者必须同时有效。
- 增加运行器所需的 Node 路径及有界脚本运行超时配置，提供默认值或明确的缺失错误；不要求用户把执行动作写入 JSON。
- 脚本相对路径按配置文件所在目录解析；运行器启动脚本时使用明确的工作目录，不依赖调用终端的位置。
- 启动时校验脚本文件存在、字段类型、快捷键格式和超时；不同 tip 可复用同一个快捷键，因为每次只绑定当前项。
- 未配置应对方案的 tip 仍只展示；配置错误给出具体字段或文件路径，不启动错误脚本。

完成标准：旧 `config.json` 仍可使用；新配置能正确关联 tip、快捷键与脚本，缺失脚本或非法绑定能得到可定位错误。

## Sensory

增强用户对提示信息的感知，负责浮窗呈现、阅读体验与交互控制。当前已有单行提示、倒计时边框、涟漪动画和窗口拖动能力。

### 视觉与阅读体验

阶段目标：提示内容可完整读取，切换动画具有足够的视觉区分。

当前优先处理顶部的两项短期需求；具体方案在实施时确定，不在此重复维护需求卡片。

### 浮窗交互控制

阶段目标：浮窗可调整位置、不阻挡下层窗口操作，并保留恢复控制与退出的入口。

候选拆分，按实际使用需要推进：

- 提供交互与鼠标穿透状态之间的切换方式。
- 保证穿透状态下仍能恢复浮窗控制。
- 提供明确的退出入口。

本期脚本触发键已明确全局生效；鼠标穿透、恢复交互和退出入口的具体控制方式仍待后续确定。此方向暂不包含完整设置中心或通用 UI
框架。

## Detection

获取用户桌面信息，识别特定场景或条件。先限定一个目标应用中的一个明确场景，再根据验证结果扩展。

### 信息获取验证

阶段目标：找到一条能够支持目标场景判定的真实信息获取路径。

- 选定目标应用，收集条件成立与不成立的例子。
- 比较窗口信息、可访问性信息、指定区域图像或应用公开接口，确定输入来源与获取范围。
- 验证信息获取的启动、停止，以及目标消失或获取失败时的处理。
- 测量延迟与资源开销，明确兼容限制，以及输入是否保存或传输。

得到可行结果后再进入识别开发；若不可行，先调整信息来源或缩小场景范围。

### 单场景识别与稳定触发

阶段目标：对一个真实场景输出可靠的条件状态，并产生可核对的提示。

- 定义匹配、不匹配与不可判定的含义，用正反样本验证误报、漏报及判定延迟。
- 明确数据有效期，避免将缺失或过期信息继续作为有效匹配。
- 处理短时抖动、持续满足、解除后重现，确定去抖、冷却与重复触发规则。
- 将条件状态连接到 Sensory，通过提示或诊断信息观察判定结果。

具体识别方法与质量阈值尚未确定。此阶段先验证识别和提示，不依赖键盘宏执行。

## Reaction

为提示或识别条件关联应对方案，通过用户确认执行输入宏。当期先实现轮转 tip 触发 TS 鼠标宏，再考虑键盘动作及 Detection 协作。

### 当期 MVP：TS 运行器

阶段目标：Rust 能直接启动用户提供的 TS 脚本，并掌握其运行生命周期。

- 用户输入的 TS 脚本仅是一个异步函数。参数为 Nyra 提供的上下文。
- 复用固定支持范围的 Node.js 运行时，明确可直接运行的 TS 语法和模块格式；约定一个导出的异步宏入口，由运行器传入 Nyra SDK。
- Rust 以参数数组启动子进程，处理脚本路径中的空格和中文；首次支持本机已配置的 Node，不自动安装工具或建设安装器。
- 每次触发创建独立运行标识和进程；入口返回并且已接收的动作完成后才报告完成。禁止未等待的动作在脚本完成后继续执行。
- 捕获脚本加载错误、未处理异常和非正常退出；超时、Nyra 正常退出或通道断开时终止本次运行，并调用输入执行器清理后回收进程。
- 运行器状态至少区分空闲、运行中、结束清理、完成和失败；完成清理前不接受下一次启动。

完成标准：有效 TS 能直接加载，修改后下次触发生效；语法/入口错误可定位；超时或失败后进程被回收且不再发出新动作请求。基本错误输出属于本期，IDE
调试集成不阻塞交付。

### 当期 MVP：SDK 与标准流协议

阶段目标：TS 用顺序异步调用表达动作，Rust 接收、执行并回传结果。

- 提供五个最小接口及类型声明：`click(x, y)`、`mouseDown(x, y)`、`mouseUp(x, y)`、`wait(ms)`、`log(message)`；动作及等待必须
  `await`，日志保证进入有序通道。
- TS 子进程 stdout 向 Rust 发送结构化请求/日志；Rust 通过子进程 stdin 返回结果。子进程 stderr 承载运行器诊断，Rust
  持续读取两个输出流。
- 使用逐行 JSON，包含协议版本、运行标识及请求标识；处理分段读取、非法消息和有限缓冲，异常时停止运行，不自动重放动作。
- Rust 维护执行顺序和运行有效性；拒绝并行动作请求及取消后的迟到请求，返回具体动作失败原因。等待由宿主执行，不能阻塞 Bevy
  主循环。
- `log` 经协议发送到 Rust，再由 Rust 写到自身 stdout，附带 tip/运行标识；运行器错误写到 Rust stderr。协议流与普通日志分开，避免日志被误解析成动作。
- SDK 与运行器一起提供，不要求宏作者自己拼 JSON 或处理管道；不扩展日志窗口、日志数据库或完整轨迹查看器。

完成标准：一段 TS 可顺序请求动作和等待，并在 Nyra 的标准输出看到脚本日志；宿主错误能传回对应调用，协议异常不会导致未知输入。

### 当期 MVP：鼠标动作与资源清理

阶段目标：Rust 执行屏幕坐标动作，并在拆分按下/释放的情况下避免正常失败路径遗留持键。

- 实现位置换算和坐标校验，支持点击、独立按下与独立释放；点击内部保持一次完整的按下/释放，必要的短按时长由执行器统一约定。
- 记录本次脚本持有的左键；拒绝重复按下和无对应按下的释放。脚本结束仍有未配对按下时尝试释放并报告错误。
- 实现以毫秒计的非阻塞、可中止等待；校验非法/过长等待，并受整段脚本运行超时约束。
- 触发时记录前台目标；意外切出目标、目标关闭、输入发送失败或运行器终止时停止后续动作并尝试释放本次持有的按钮。不自动抢回焦点或追踪新目标窗口。
- 启动时若用户仍物理按住左键则拒绝执行；不主动释放用户已有的按下状态。清理失败必须报告，不能把进程退出等同于输入已清理。
- 在实际目标程序验证输入是否接受；本期不承诺后台输入、绝对焦点隔离或进程被强杀后的清理。

完成标准：指定坐标点击有效；按下、等待、异位置释放能按顺序执行；失败、超时和正常退出路径停止后续输入并尝试释放持键，错误有明确反馈。

### 当期 MVP：端到端验收

阶段目标：玩家通过配置和脚本完成“tip 受关注 → 全局按键 → TS 执行动作 → Rust 输出日志”。

- 用一个有实际用途的脚本串联日志、点击、按下、等待和释放；在目标程序确认动作顺序、位置与等待效果。
- 用带脚本 tip 与纯提示轮转，核对绑定出现/注销、同键复用、旧事件失效，以及脚本执行跨越 tip 切换的归属。
- 检查长按与重复触发、脚本错误、未配对按下、超时和退出清理；记录输入发送完成，不将其直接称为游戏业务成功。
- 随实现提供最小 `config.json` 示例和宏使用说明，说明坐标系、Node 前提、日志查看方式及运行限制；不在本规划任务中生成可执行脚本或测试代码。

完成标准：五类接口均在真实脚本中走通，只有当前 tip 能启动其脚本，运行结果及日志可核对；所有以上验收结果按实测记录，未验证的目标兼容性明确保留。

### 后续：键盘动作扩展

阶段目标：一套有实际用途的宏可以手动启动、停止，并反馈执行结果。

- 选择目标程序和一套应对方案，表达其适用场景、按键顺序、组合键与等待时间。
- 明确宏的配置方式、键位解释及支持的动作范围，并检查无效配置。
- 提供启动与停止入口，取消后停止后续输入并释放宏持有的按键。
- 处理焦点变化、目标关闭、重复启动和发送失败，避免后续输入进入错误窗口。
- 区分成功、取消与失败；按键发送完成不直接等同于业务动作成功。

此阶段可在当期 TS 执行与生命周期管理基础上扩展，并独立于 Detection 推进；暂不建设自定义通用脚本语言或自动策略生成能力。

### 条件驱动应对

阶段目标：一组真实条件能够关联正确方案，并完成从识别到动作结果的验证。

- 连接 Detection 的条件状态与对应方案，通过 Sensory 呈现方案及执行反馈。
- 沿用用户确认触发宏的方式；条件成立后的自动执行不属于当前规划。
- 执行前检查条件有效期和目标状态，处理条件失效、方案冲突与重复触发。
- 验证条件观测、方案呈现、宏执行及结果反馈的完整过程。

该阶段依赖稳定的条件识别和独立宏执行能力。首次限定一个场景与一套宏，不扩展到复杂多条件编排。

## 技术债

### 动画历史状态与任务调度解耦

- [ ] 将 `ActiveJob.anim_ripple_rng_at` 及起点采样逻辑迁移到动画层。

优先级：低，暂不阻塞近期需求。当前耦合范围有限，但继续向 `ActiveJob` 添加动画缓存会模糊调度与表现层的职责。

已知行为缺口：当前最小间隔约束针对相邻任务生成的起点；重复展示同一提示而跳过动画时，历史值仍会更新，因此顶部已勾选的动画需求尚未覆盖连续两次实际播放的全部情况。

建议由现有 `Ripple` 组件保存上一次实际使用的起点，在 `begin_tip_transition` 确认启动动画时采样，并随启动更新历史；
`ActiveJob` 仅保留任务索引、预览索引和时间信息。后续扩展动画状态或处理实际播放起点过近的问题时，优先偿还此债。

完成标准：调度层不再持有动画起点或依赖动画随机采样；跳过动画不改变历史起点；连续实际启动的动画沿闭合边框的最短起点间隔至少为周长的
25%。

## 术语与维护方式

- **Epic（大方向）**：持续建设的产品能力，对应 Sensory、Detection、Reaction 三个大章节。
- **Milestone（阶段目标）**：大方向下可实际验证的一步，对应各章中的小标题。
- **短期需求**：准备近期实施的小目标，在顶部用一行文本表达；按排列顺序体现优先级。
- **优先级**：影响当前使用或阻断当前工作的问题优先，其次是已选定的短期需求；其余内容保留在所属大方向下，准备实施时再提取到顶部。
- **进度**：未完成使用未勾选项，完成并验证后移入下方记录；需要时在行末注明开发中、待验证或阻塞原因。

大方向按需要逐步拆分，不必提前细化所有方案。准备实施时，仅将选中的具体小目标加入顶部；长期拆分内容不整体搬入待开发清单。方案确定后直接更新所属章节，不建立额外编号或登记表。

## 已完成需求

本路线图中的新增需求尚无完成项。既有能力见[一阶段实现总结](docs/phase1-summary.md)，当前使用方式见 [README](README.md)。
